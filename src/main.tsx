import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutesComponets from './route'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <RoutesComponets />
  </React.StrictMode>
)
